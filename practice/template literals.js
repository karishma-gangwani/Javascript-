// linebreaks can be added just with enter. they keep the white space.
// can embed variables and expressions into strings.
// concatenation not required.
let word1 = "Dylan";
let word2 = "Israel";

const fullName = `${word1} ${word2}`;

console.log(fullName);
